name: Test_CDC_Audit_Timeline_Service

services:
  postgres:
    image: postgres:17
    env_file:
      - postgres.test.env
    ports:
      - "5435:5432"
    volumes: 
      - postgres_test_data:/var/lib/postgresql/data
  
volumes:
  postgres_test_data:
